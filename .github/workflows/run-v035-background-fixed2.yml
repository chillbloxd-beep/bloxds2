name: Run second corrected v0.3.5 background migration

on:
  push:
    branches:
      - feature/reliability-performance-v035

permissions:
  contents: write

jobs:
  patch:
    if: ${{ !contains(github.event.head_commit.message, 'Apply v0.3.5 background patch v2') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/reliability-performance-v035
          fetch-depth: 0
      - name: Apply corrected migration
        run: python scripts/run-v035-background-fixed2.py
      - name: Remove all one-time migration files
        run: |
          rm -f scripts/apply-v035-background.py scripts/run-v035-background-fixed.py scripts/run-v035-background-fixed2.py
          rm -f .github/workflows/apply-v035-background.yml .github/workflows/run-v035-background-fixed.yml .github/workflows/run-v035-background-fixed2.yml
      - name: Commit migration
        run: |
          git config user.name "OneBlock Analytics CI"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Apply v0.3.5 background patch v2"
          git push origin HEAD:feature/reliability-performance-v035
